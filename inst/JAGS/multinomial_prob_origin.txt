model{
# likelihood
for(i in 1:nProbO){
  pop[i] ~ dcat(probO[i, 1:npop])
  assT[i] ~ dcat(psi[pop[i], 1:ndest])
}

# priors

for(i in 1:npop){
  for(k in 1:ndest){
    m0[i, k] ~ dgamma(1, 1)
    psi[i, k] <- m0[i, k] / sum(m0[i, 1:ndest])
  } #k
}#i
}
